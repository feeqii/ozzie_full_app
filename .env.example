SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Do not put secrets in `.env` for the Flutter app.
# Recitation scoring is server-side; no OpenAI key should ship to clients.
